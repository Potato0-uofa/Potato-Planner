<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:gravity="center_vertical"
    android:orientation="vertical"
    android:padding="32dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="32dp"
        android:text="Would you like to enable Notifications for events?"
        android:textAlignment="center"
        android:textColor="#FFFFFF"
        android:textSize="31sp"
        android:textStyle="bold"
        android:translationY="-135dp" />

    <ImageView
        android:id="@+id/notification_bell_icon"
        android:layout_width="230dp"
        android:layout_height="230dp"
        android:translationX="60dp"
        android:translationY="-100dp"
        app:srcCompat="@drawable/notificationimage" />

    <Button
        android:id="@+id/enable_notification_button"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:backgroundTint="#7EB765"
        android:text="Enable"
        android:textAllCaps="false"
        android:textColor="#365727"
        android:textSize="20sp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/disable_notification_button"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:backgroundTint="#B85151"
        android:text="Disable"
        android:textAllCaps="false"
        android:textColor="#501414"
        android:textSize="20sp"
        android:textStyle="bold"
        android:translationY="50dp" />

</LinearLayout>